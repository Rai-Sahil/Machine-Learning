It uses the the basic math formula of Line of Best Fit. Since it makes the line of best fit using training data on graph and then predicts the value using that line.

```
y = m * area * b

area = Value inputted to predict
m = Slope
area = X coordinate
```
